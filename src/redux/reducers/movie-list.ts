import { ADD_MOVIE } from '../movie-list/action-types';

const initialState = {
  allMovieIds: [],
  moviesMapById: {}
};

export default (state = initialState, action) => {
  switch (action.type) {
    case ADD_MOVIE:

  }
}